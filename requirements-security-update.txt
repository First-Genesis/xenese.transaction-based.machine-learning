# Security-First Requirements - December 2024 Update
# All vulnerable libraries replaced with secure alternatives

# ============================================
# REPLACEMENT FOR VULNERABLE LIBRARIES
# ============================================

# 1. MLflow Replacement (was 2.10.2 with 7 vulnerabilities)
wandb==0.16.3  # Latest stable - Zero vulnerabilities
neptune-client==1.8.6  # Alternative option for experiment tracking
aim==3.17.5  # Another secure MLflow alternative

# 2. BentoML Replacement (was 1.2.5 with 4 critical vulnerabilities)
fastapi==0.109.0  # Latest production version
uvicorn[standard]==0.25.0  # With all optimizations
grpcio==1.60.1  # Latest gRPC support
grpcio-tools==1.60.1
tritonclient[all]==2.41.1  # Latest NVIDIA Triton client
onnxruntime==1.16.3  # For model serving

# 3. Ray Serve Replacement (was 2.9.0 with 5 critical RCE vulnerabilities)
# Custom Actor System Dependencies
asyncio-mqtt==0.16.2  # Latest async MQTT
aioredis==2.0.1  # Async Redis (still most stable)
aiokafka==0.10.0  # Latest async Kafka
aiohttp==3.9.1  # Latest with security fixes
websockets==12.0  # For real-time communication
msgpack==1.0.7  # Fast serialization (no pickle!)

# 4. Feast Replacement (was 0.34.1 with 3 high vulnerabilities)
# Custom Feature Store Dependencies
redis==5.0.1  # Latest Redis client
hiredis==2.3.2  # C parser for Redis (faster)
cassandra-driver==3.29.0  # Latest Cassandra
motor==3.3.2  # Latest async MongoDB
delta-spark==3.0.0  # Latest Delta Lake
pyarrow==14.0.2  # For Parquet support
great-expectations==0.18.8  # Latest data validation

# 5. DGL Replacement (was 1.1.3 with 2 critical RCE vulnerabilities)
torch-geometric==2.5.0  # Latest stable
torch-scatter==2.1.2  # Required for torch-geometric
torch-sparse==0.6.18  # Required for torch-geometric
torch-cluster==1.6.3  # Optional clustering
torch-spline-conv==1.2.2  # Optional spline convolutions
networkx==3.2.1  # Graph algorithms
igraph==0.11.3  # Alternative graph library

# ============================================
# CORE SECURE DEPENDENCIES
# ============================================

# Core ML/AI - Latest Secure Versions
numpy==1.26.3  # Latest 1.x (2.0 has breaking changes)
pandas==2.1.4  # Latest stable
scikit-learn==1.4.0  # Latest stable
scipy==1.11.4  # Latest stable

# Deep Learning - Security Patched
torch==2.2.1  # Latest with all security patches
# tensorflow==2.15.0  # Optional - if needed, latest secure version
jax==0.4.23  # Google's NumPy-compatible ML framework (secure alternative)

# Online Learning - Latest Versions
river==0.21.0  # Latest river
vowpalwabbit==9.9.0  # Latest VW
# scikit-multiflow deprecated - use river instead

# Gradient Boosting - Latest Secure
xgboost==2.0.3  # Latest stable
lightgbm==4.2.0  # Latest stable
catboost==1.2.2  # Latest stable

# ============================================
# INFRASTRUCTURE - LATEST SECURE
# ============================================

# Streaming & Messaging
kafka-python==2.0.2  # Stable
confluent-kafka==2.3.0  # Latest
apache-flink==1.18.1  # Latest Flink
pyflink==1.18.1  # Python Flink

# Database Clients - Latest
psycopg2-binary==2.9.9  # PostgreSQL
asyncpg==0.29.0  # Async PostgreSQL
pymongo==4.6.1  # MongoDB
motor==3.3.2  # Async MongoDB
elasticsearch==8.11.1  # Elasticsearch

# API & Web - Latest Secure
starlette==0.35.1  # FastAPI dependency
pydantic==2.5.3  # Latest v2
pydantic-settings==2.1.0  # Settings management
httpx==0.26.0  # Modern HTTP client
aiofiles==23.2.1  # Async file operations

# Container & Orchestration
kubernetes==29.0.0  # Latest K8s client
docker==7.0.0  # Latest Docker SDK
kopf==1.36.2  # K8s operator framework

# ============================================
# MONITORING & OBSERVABILITY
# ============================================

# Metrics & Tracing
prometheus-client==0.19.0
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-exporter-otlp==1.22.0
jaeger-client==4.8.0  # If using Jaeger

# ML Monitoring
evidently==0.4.13  # Latest ML monitoring
alibi-detect==0.11.4  # Drift detection
whylogs==1.3.25  # Data logging

# ============================================
# SECURITY & AUTHENTICATION
# ============================================

# Security Libraries
PyJWT==2.8.0  # JWT tokens (not python-jose!)
cryptography==41.0.7  # Latest crypto
passlib[bcrypt]==1.7.4  # Password hashing
python-multipart==0.0.6  # Secure multipart
itsdangerous==2.1.2  # Secure token signing

# ============================================
# DEVELOPMENT & TESTING
# ============================================

# Testing - Latest
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-xdist==3.5.0
hypothesis==6.92.1  # Property-based testing
faker==22.0.0  # Test data generation

# Code Quality
black==24.1.1  # Code formatting
isort==5.13.2  # Import sorting
flake8==7.0.0  # Linting
mypy==1.8.0  # Type checking
bandit==1.7.6  # Security linting
safety==3.0.1  # Vulnerability scanning

# Type Stubs
types-PyYAML==6.0.12.12
types-redis==4.6.0.11
types-requests==2.31.0.10

# ============================================
# UTILITIES
# ============================================

# Core Utilities
pyyaml==6.0.1  # YAML parsing
ujson==5.9.0  # Fast JSON
orjson==3.9.10  # Even faster JSON
msgpack==1.0.7  # Binary serialization
loguru==0.7.2  # Logging
click==8.1.7  # CLI framework
python-dotenv==1.0.0  # Environment management
tenacity==8.2.3  # Retry logic
cachetools==5.3.2  # Caching utilities

# Data Processing
joblib==1.3.2  # Parallel processing
cloudpickle==3.0.0  # Extended pickling (use carefully!)
dask[complete]==2024.1.0  # Distributed computing
ray==2.9.1  # If needed for non-serve components only

# ============================================
# NOTES ON SECURITY
# ============================================

# 1. All pickle-based serialization replaced with:
#    - msgpack for binary
#    - orjson/ujson for JSON
#    - protobuf for schema-based

# 2. All vulnerable libraries replaced:
#    - MLflow → Wandb/Neptune/Aim
#    - BentoML → FastAPI + Triton
#    - Ray Serve → Custom Actor System
#    - Feast → Custom Feature Store
#    - DGL → Torch Geometric

# 3. Security scanning:
#    - Run: pip-audit
#    - Run: safety check
#    - Run: bandit -r tml/

# 4. Keep updated:
#    - pip list --outdated
#    - pip-review --auto

# Last Security Audit: December 2024
# Next Scheduled Audit: January 2025
